export interface GlobalDataSummary{
    country ?: string,    //Can add ? after each variable country?: str"
    confirmed ?: number,
    deaths ?: number,
    recovered ?: number,
    active ?: number
}